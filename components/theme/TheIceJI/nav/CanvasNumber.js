import styled from "@emotion/styled";
import { motion } from "framer-motion"

const CanvasNumber = styled(motion.h6)`
  font-family: poppins;
  font-weight: 800;
  color: #fff;
  opacity: 0.5;
`;

export default CanvasNumber;